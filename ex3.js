//find area of triangle
// formula (base * hight)/2;

var base = prompt("please enter the base value");
var height = prompt("please enter the hight value ");
var area = (base * height) / 2;
document.write("base:" + base + "height:" + height + "area:" + area);
